# KL Scents E-Commerce App

## How to start coding
1. Run `npm install` to grab all the packages (React, Tailwind, Supabase, Lucide).
2. Create a `.env.local` file and paste in the Supabase keys (DM me for these).
3. Run `npm run dev` to start the local server.
